[project]
name = "akcool_deps"
version = "0.0.1"
dependencies = [
    "pycups"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[deploy.dependencies.apt]
packages = [
    "cups",
    "libcups2-dev",
    "gcc",
    "python3-dev"
]
